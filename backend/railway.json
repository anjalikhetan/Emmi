{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn api.wsgi:application --bind 0.0.0.0:$PORT --settings=api.settings.production_simple"
  }
}
